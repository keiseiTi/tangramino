{
  "name": "@tangramino/engine",
  "version": "0.1.0",
  "description": "A schema engine that write logic",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./types/index.d.ts",
  "type": "module",
  "files": [
    "src",
    "types"
  ],
  "scripts": {
    "build": "rm -rf dist && rollup -c && tsc -p tsconfig.json",
    "watch": "rollup --watch -c && tsc -w -p tsconfig.json"
  },
  "keywords": [
    "schema",
    "engine"
  ],
  "author": "yupeilin <yuchacha250@gmail.com>",
  "license": "MIT ",
  "dependencies": {
    "immer": "^10.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-typescript": "^12.1.4",
    "rollup": "^4.46.0",
    "tslib": "^2.8.1",
    "typescript": "^5.9.0"
  }
}
